func main() {
	defer func() {
		if r := recover(); r != nil {
			fmt.Fprintln(os.Stderr, r)
			os.Exit(1)
		}
	}()
{{range .Locals}}	var {{.}} rt.Value
{{end}}{{range .Funcs}}	{{.}} = _fn_{{.}}
	globals[{{printf "%q" .}}] = {{.}}
{{end}}{{.Body}}}
